import React from 'react';
import Header from '../../Shared/Header/Header';

const Courses = () => {
    return (
        <div>
            <Header></Header>
            This is corses page
        </div>
    );
};

export default Courses;