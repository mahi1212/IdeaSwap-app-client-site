import React from 'react';
import Header from '../Shared/Header/Header';

const PageNotFound = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default PageNotFound;